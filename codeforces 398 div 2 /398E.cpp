#include<bits/stdc++.h>

using namespace std;

int main()
{
    long long n,m;
    cin>>n>>m;
    int input;
    vector<int> cards;
    long long odd=0,even=0;
    for(long long i=0;i<n;i++)
    {
        cin>>input;
        vector.push_back(input);
        if(input%2==0)
        even++;
        else
        odd++;
    }
        
    return 0;
}
